Updated repository to overwrite main with Minimal portfolio homepage and a Projects index that links to generated project case-study scaffolds and SVG thumbnails. The site uses projects.json and thumbnails in public/thumbnails. Run the existing generate-projects.js if you want alternative outputs (MDX scaffolds / SVGs).